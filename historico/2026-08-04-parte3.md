# Snapshot - 04/08/2026 (parte 3)

Quarta sessão do dia. **Módulo 2 (Compromissos avulsos) concluído e validado com as chaves reais.**

## 🔑 Chaves recebidas

- `ANTHROPIC_API_KEY` — colada direto pelo Marcelo no chat (a do vida-de-trader era Sensitive na Vercel, confirmei ao vivo com `vercel env pull` que ela devolve vazia mesmo autenticado — não tem contorno técnico, só revelar manual no dashboard ou criar nova. Marcelo optou por gerar uma nova.)
- `GROQ_API_KEY` — também colada direto. Achei uma chave real já em uso no Nômade Trader antes de perguntar; Marcelo preferiu gerar uma dedicada.
- Salvei no `.env` sem re-exibir os valores no chat depois de gravados.

## 🎯 O que foi feito

- `src/lib/ia/anthropic.ts` — singleton do cliente Claude
- `src/lib/ia/compromissos.ts` — `extrairCompromisso()`: tool-use FORÇADO (não "responda em JSON") pra tirar `{titulo, data, hora, local, confianca, ambiguo, pergunta}` de texto livre. Prompt de sistema dá o momento atual no fuso do usuário como referência, pra resolver "amanhã", "sexta", "daqui a 2h" etc.
- `src/lib/ia/transcricao.ts` — `transcreverAudio()` via Groq Whisper large v3 (multipart, `response_format=text`)
- `src/lib/datas.ts` ganhou `horaLocalParaUtc()` (usa `date-fns-tz`, já era dependência) e `dataHoraCurta()`
- `POST /api/compromissos/interpretar` — aceita texto OU áudio (FormData), devolve o rascunho **sem persistir** — a gravação só acontece depois que você confirma
- `src/app/compromissos/acoes.ts` — `confirmarCompromissoAction`, `concluirCompromissoAction`, `cancelarCompromissoAction`
- `NovoCompromisso.tsx` — caixa de entrada com texto **e gravação de áudio de verdade** (MediaRecorder do navegador), card de revisão editável antes de confirmar
- `ListaCompromissos.tsx` — próximos, com concluir/cancelar otimista
- `/compromissos` — página completa
- Home ganhou o badge de compromissos ("X hoje" ou "próx. DD/MM, HH:mm")
- Barra de topo ganhou os 4 módulos (antes só tinha Apex e Mercado)

## ✅ Validado com chamadas REAIS às APIs (não mock)

1. **Gerei um áudio sintético de verdade** (`say` do macOS + `afconvert` pra .m4a) falando "Marca pra eu ligar pro João amanhã às três da tarde"
2. **Testei o Groq isolado via curl** — transcreveu perfeito
3. **Testei a extração isolada** (`extrairCompromisso` chamado direto, sem servidor) — resolveu "amanhã às três da tarde" pra `2026-08-05T15:00` corretamente, e um segundo teste com texto vago ("preciso resolver aquilo com o pessoal") disparou `ambiguo=true` com uma pergunta de volta no tom certo
4. **Testei a rota inteira via curl** com o áudio real — devolveu texto + transcrição + rascunho num payload só
5. **Testei a UI no navegador**: mandei texto ("marca reunião com o contador sexta às 10h") → "sexta" resolveu pra 07/08 (a sexta certa daquela semana) → confirmei → apareceu na lista → **conferido no banco**: `quando: 2026-08-07T13:00:00Z` = 10:00 em São Paulo, conversão de fuso correta
6. Testei o caso ambíguo na UI também — a pergunta de volta apareceu com o tom descontraído esperado
7. Limpei os dois compromissos de teste do banco no final

## 🎙️ Sobre o teste de áudio pela UI

A gravação por microfone do navegador (MediaRecorder) **não dá pra automatizar com as ferramentas disponíveis aqui** (não hà microfone real no ambiente de teste). O que validei em troca: a rota que o botão de gravar chama é a MESMA que testei via curl com áudio real — mesmo código, sem atalho. A parte específica do navegador (pedir permissão, gravar, montar o Blob) é API padrão da Web, de baixo risco. **Vale você testar isso no celular/PC de verdade na próxima vez que abrir o app** — é o único pedaço do Módulo 2 que eu não consegui apertar o botão de gravar sozinho.

## 🐙 Git remoto configurado

Marcelo criou `github.com/Marcelo210598/socrates` (vazio) e eu conectei como `origin`. Deploy na Vercel vai ler daqui.

## 📊 Estado atual do projeto

- Fundação: **100%**
- **Módulos 1, 2 e 5: 100% na web.** (1=Tarefas, 2=Compromissos, 5=Apex)
- Módulos 3, 4: **0%** (schema pronto)
- `tsc --noEmit` limpo, `eslint` limpo

## 🚧 Próxima sessão (To-do)

1. **Módulo 3 (Motivacional)** — não depende de chave nova; já tem tarefas, compromissos e o banco de frases. É sobre religar os pontos: quando algo passa da hora sem check, gerar/sortear uma cobrança.
2. **Módulo 4 (Briefing)** — precisa decidir a fonte do calendário econômico (Forex Factory JSON) e do candle de referência (Yahoo Finance) — já usados no Vida de Trader, dá pra copiar o padrão de lá.
3. Bot do Telegram — ainda falta o `TELEGRAM_BOT_TOKEN`. Com os módulos web prontos, o bot vira "só" plugar o mesmo `extrairCompromisso`/ações num roteador de mensagens em vez de formulário.
4. Login (NextAuth + Google) pra aposentar o `obterUsuarioAtual()` temporário
5. Testar a gravação de áudio pelo microfone de verdade (celular ou PC)
6. Worker do Railway + deploy na Vercel

## 💡 Observações pra retomar rápido

- `.env` agora tem `ANTHROPIC_API_KEY` e `GROQ_API_KEY` reais — funcionando, testado
- Scripts de conferência no banco: rodar de DENTRO da pasta do projeto com `node --env-file=.env --import tsx arquivo.ts` (o `.env` não carrega sozinho fora do Next)
- Regra nova: extração de IA usa **tool-use forçado**, nunca "responda em JSON solto" — schema garante o formato
- Padrão novo: fluxo de IA **nunca persiste antes da sua confirmação** — a rota `/api/.../interpretar` só devolve rascunho; salvar é uma ação separada
